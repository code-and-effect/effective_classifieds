EffectiveClassifieds.setup do |config|
  config.classifieds_table_name = :classifieds

  # Layout Settings
  # Configure the Layout per controller, or all at once
  # config.layout = { application: 'application', admin: 'admin' }

  # Membership Categories Settings
  # Configure the class responsible for the classified.
  # config.classified_class_name = 'Effective::Classified'
end
